{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile and run",
      "type": "shell",
      "command": "javac '${file}' ; Get-Content input.txt | java ${fileBasenameNoExtension} > output.txt",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
